---
title: QCustomDrawer
description: A slide-in side panel / sheet.
---

import useBaseUrl from '@docusaurus/useBaseUrl';
import Zoomable from '@site/src/components/Zoomable';

{/* generated:widget-reference */}

# QCustomDrawer

<Zoomable src={useBaseUrl('/img/showcase/drawer.gif')} alt="QCustomDrawer" />

A slide-in side panel / sheet.

A dimmed overlay with a panel that slides in from an edge (left / right /
top / bottom). Backdrop-click or Esc closes it. Tokenized.

drawer = QCustomDrawer(window, side="left", size=280)
drawer.contentLayout().addWidget(nav)
drawer.open()

## At a glance

| | |
|---|---|
| **Tier** | Free (GPLv3) |
| **Import** | `from Custom_Widgets.QCustomDrawer import QCustomDrawer` |
| **Qt Designer** | Yes — drag it from the palette |

## Quick start

```python
from Custom_Widgets.QCustomDrawer import QCustomDrawer

widget = QCustomDrawer()
```

## Dark theme

Colours come from the design tokens, so the widget follows the app theme with no extra work.

<Zoomable src={useBaseUrl('/img/showcase/drawer-dark.gif')} alt="QCustomDrawer in dark theme" />

## Properties

Every property below is settable in code and in Qt Designer.

| Property | Type | Default |
|---|---|---|
| `side` | `enum: `left` / `right` / `top` / `bottom`` | `left` |
| `drawerSize` | `int` | — |

## Signals

| Signal |
|---|
| `closed()` |
| `opened()` |

## Methods

| Method | Description |
|---|---|
| `addWidget(widget)` | Add a widget. |
| `close()` | Close. |
| `closed(...)` | Closed. |
| `contentLayout()` | Content layout. |
| `drawerSize(*args, **kwargs)` | Drawer size. |
| `isOpen()` | Return whether the widget is open. |
| `open()` | Open. |
| `opened(...)` | Opened. |
| `side(*args, **kwargs)` | Side. |

## Theming

Colours come from the design tokens, so they follow the active theme. Roles used: `surface`, `on-surface`, `outline`.

See [Design tokens](../02-Theming/DesignTokens.md).

## Related

[QCustomBreadcrumbs](QCustomBreadcrumbs.mdx) · [QCustomCommandPalette](QCustomCommandPalette.mdx) · [QCustomHamburgerMenu](QCustomHamburgerMenu.mdx) · [QCustomHeaderNav](QCustomHeaderNav.mdx) · [QCustomMenu](QCustomMenu.mdx) · [QCustomPagination](QCustomPagination.mdx) · [QCustomSidebar](QCustomSidebar.md) · [QCustomSidebarButton](QCustomSidebarButton.md)
