---
mdx:
  format: md
---

<!-- generated:widget-reference -->
# QCustomTabWidget

![QCustomTabWidget screenshot](/img/showcase/tabwidget.png)

`QCustomTabWidget` — a tokenized tab container.

Built on QTabWidget (correct tab management + keyboard) with three
token-styled looks via the `tabStyle` property: "underline" (default),
"pills", "enclosed". Density follows `sizeVariant`.

---

## Import

```python
from Custom_Widgets.QCustomTabWidget import QCustomTabWidget
```

Also available from the **Qt Designer** palette — every property below is settable in Designer and saved into the `.ui` file.

## Constructor

```python
QCustomTabWidget(parent=None)
```

## Properties

| Property | Type | Default |
|---|---|---|
| `tabPosition` | `QTabWidget::TabPosition` | — |
| `tabShape` | `QTabWidget::TabShape` | — |
| `currentIndex` | `int` | — |
| `count` | `int` | — |
| `iconSize` | `QSize` | — |
| `elideMode` | `Qt::TextElideMode` | — |
| `usesScrollButtons` | `bool` | — |
| `documentMode` | `bool` | — |
| `tabsClosable` | `bool` | — |
| `movable` | `bool` | — |
| `tabBarAutoHide` | `bool` | — |
| `closableTabs` | `bool` | `False` |
| `showAddButton` | `bool` | `False` |
| `tabStyle` | `enum: `underline` / `pills` / `enclosed`` | `underline` |
| `sizeVariant` | `enum: `sm` / `md` / `lg`` | `md` |

## Methods

| Method | Description |
|---|---|
| `addTabRequested(...)` |  |
| `closableTabs(*args, **kwargs)` |  |
| `showAddButton(*args, **kwargs)` |  |
| `sizeVariant(*args, **kwargs)` |  |
| `tabStyle(*args, **kwargs)` |  |

## Signals

| Signal | Description |
|---|---|
| `addTabRequested()` |  |

## Theming

Colours come from the design tokens, so they follow the active theme. Roles used: `surface`, `on-surface`, `surface-muted`, `outline`, `accent`, `on-primary`.

See [Design tokens](../02-Theming/DesignTokens.md).

### Dark theme

![QCustomTabWidget dark](/img/showcase/tabwidget-dark.png)

---

<!-- Generated by tools/gen_widget_docs.py from the widget's __catalog__, metaObject and module docstring. Edit the widget, then regenerate — hand edits here are overwritten. Remove the marker at the top of this file to take it over by hand. -->
