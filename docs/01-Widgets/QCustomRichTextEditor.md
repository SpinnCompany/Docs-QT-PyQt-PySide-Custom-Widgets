---
mdx:
  format: md
---

<!-- generated:widget-reference -->
# QCustomRichTextEditor

![QCustomRichTextEditor screenshot](/img/showcase/richtexteditor.png)

`QCustomRichTextEditor` — a WYSIWYG rich-text editor.

A formatting toolbar (bold / italic / underline / headings / lists /
alignment / text colour / clear) over a QTextEdit. Tokenized. Exposes
toHtml / setHtml / toPlainText / setPlainText and a `textChanged` signal.

---

## Import

```python
from Custom_Widgets.QCustomRichTextEditor import QCustomRichTextEditor
```

Also available from the **Qt Designer** palette — every property below is settable in Designer and saved into the `.ui` file.

## Constructor

```python
QCustomRichTextEditor(parent=None)
```

## Methods

| Method | Description |
|---|---|
| `editor()` |  |
| `setHtml(html)` |  |
| `setPlainText(text)` |  |
| `textChanged(...)` |  |
| `toHtml()` |  |
| `toPlainText()` |  |

## Signals

| Signal | Description |
|---|---|
| `textChanged()` |  |

## Theming

Colours come from the design tokens, so they follow the active theme. Roles used: `surface`, `on-surface`, `surface-muted`, `outline`, `accent`, `on-primary`.

See [Design tokens](../02-Theming/DesignTokens.md).

### Dark theme

![QCustomRichTextEditor dark](/img/showcase/richtexteditor-dark.png)

---

<!-- Generated by tools/gen_widget_docs.py from the widget's __catalog__, metaObject and module docstring. Edit the widget, then regenerate — hand edits here are overwritten. Remove the marker at the top of this file to take it over by hand. -->
