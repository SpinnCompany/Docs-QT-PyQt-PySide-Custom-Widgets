---
mdx:
  format: md
---

<!-- generated:widget-reference -->
# QCustomSocialButton

![QCustomSocialButton screenshot](/img/showcase/socialbutton.png)

`QCustomSocialButton` — a brand-coloured sign-in / share button.

"Continue with GitHub", "Share on X". The brand colour is the whole point,
so the widget ships a small table of them and picks the readable foreground
automatically rather than making every caller look up a hex value and then
get the contrast wrong.

The brand MARK is deliberately not drawn: reproducing a company logo is a
trademark question, not a painting one, so the caller supplies an icon and
the widget supplies the colour, shape and layout. `brandColor` is exposed so
an unlisted brand still works.

Emits clicked().

---

## Import

```python
from Custom_Widgets.QCustomSocialButton import QCustomSocialButton
```

Also available from the **Qt Designer** palette — every property below is settable in Designer and saved into the `.ui` file.

## Constructor

```python
QCustomSocialButton(parent=None, brand='github', text=None, icon=None)
```

## Properties

| Property | Type | Default |
|---|---|---|
| `brand` | `string` | `github` |
| `text` | `string` | — |
| `iconPath` | `string` | — |
| `variant` | `enum: `solid` / `outline` / `soft`` | `solid` |
| `shape` | `enum: `rounded` / `pill` / `square`` | `rounded` |
| `sizeVariant` | `enum: `sm` / `md` / `lg`` | `md` |
| `iconOnly` | `bool` | `False` |
| `brandColor` | `color` | `#24292f` |
| `surfaceColor` | `color` | `#ffffff` |

## Methods

| Method | Description |
|---|---|
| `brand(*args, **kwargs)` |  |
| `brandColor(*args, **kwargs)` |  |
| `brandNames()` |  |
| `clicked(...)` |  |
| `foregroundColor()` | Readable text colour for the current fill. |
| `iconOnly(*args, **kwargs)` |  |
| `iconPath(*args, **kwargs)` |  |
| `isKnownBrand()` |  |
| `setBrand(brand)` | Adopt a known brand's colour and default caption. |
| `shape(*args, **kwargs)` |  |
| `sizeVariant(*args, **kwargs)` |  |
| `surfaceColor(*args, **kwargs)` |  |
| `text(*args, **kwargs)` |  |
| `variant(*args, **kwargs)` |  |

## Signals

| Signal | Description |
|---|---|
| `clicked()` |  |

## Theming

Colours come from the design tokens, so they follow the active theme. Roles used: `surface`, `on-surface`.

See [Design tokens](../02-Theming/DesignTokens.md).

### Dark theme

![QCustomSocialButton dark](/img/showcase/socialbutton-dark.png)

## Example

A runnable demo ships with the library:

```bash
python examples/PySide6/QCustomSocialButton/main.py
```

---

<!-- Generated by tools/gen_widget_docs.py from the widget's __catalog__, metaObject and module docstring. Edit the widget, then regenerate — hand edits here are overwritten. Remove the marker at the top of this file to take it over by hand. -->
