---
mdx:
  format: md
---

<!-- generated:widget-reference -->
# QCustomPopover

![QCustomPopover screenshot](/img/showcase/popover.png)

`QCustomPopover` — a rich popover anchored to a widget.

Unlike a text tooltip, a popover holds arbitrary widgets and points at its
anchor with an arrow. Opens on demand (or wire it to a trigger via
attach()); closes on outside click. Tokenized colours via qproperty.

---

## Import

```python
from Custom_Widgets.QCustomPopover import QCustomPopover
```

## Constructor

```python
QCustomPopover(anchor=None, placement='bottom')
```

## Properties

| Property | Type | Default |
|---|---|---|
| `panelColor` | `color` | — |
| `borderColor` | `color` | — |

## Methods

| Method | Description |
|---|---|
| `addWidget(widget)` |  |
| `attach(trigger, placement='bottom')` | Create a popover anchored to `trigger` and open it on click. |
| `borderColor(*args, **kwargs)` |  |
| `closed(...)` |  |
| `contentLayout()` |  |
| `opened(...)` |  |
| `panelColor(*args, **kwargs)` |  |
| `setPlacement(placement)` |  |
| `showPopover()` |  |

## Signals

| Signal | Description |
|---|---|
| `closed()` |  |
| `opened()` |  |

## Theming

Colours come from the design tokens, so they follow the active theme. Roles used: `surface`, `on-surface`, `outline`.

See [Design tokens](../02-Theming/DesignTokens.md).

### Dark theme

![QCustomPopover dark](/img/showcase/popover-dark.png)

---

<!-- Generated by tools/gen_widget_docs.py from the widget's __catalog__, metaObject and module docstring. Edit the widget, then regenerate — hand edits here are overwritten. Remove the marker at the top of this file to take it over by hand. -->
