---
title: QCustomSkeleton
description: A shimmering loading placeholder.
---

import useBaseUrl from '@docusaurus/useBaseUrl';
import Zoomable from '@site/src/components/Zoomable';

{/* generated:widget-reference */}

# QCustomSkeleton

<Zoomable src={useBaseUrl('/img/showcase/skeleton.gif')} alt="QCustomSkeleton" />

A shimmering loading placeholder.

A rounded shape (line / rect / circle) with a moving shimmer, shown while
real content loads. Colours come from tokens via qproperty (baseColor /
highlightColor set by skeleton_qss).

## At a glance

| | |
|---|---|
| **Tier** | Free (GPLv3) |
| **Import** | `from Custom_Widgets.QCustomSkeleton import QCustomSkeleton` |
| **Qt Designer** | Yes — drag it from the palette |

## Quick start

```python
from Custom_Widgets.QCustomSkeleton import QCustomSkeleton

widget = QCustomSkeleton()
```

## Dark theme

Colours come from the design tokens, so the widget follows the app theme with no extra work.

<Zoomable src={useBaseUrl('/img/showcase/skeleton-dark.gif')} alt="QCustomSkeleton in dark theme" />

## Properties

Every property below is settable in code and in Qt Designer.

| Property | Type | Default |
|---|---|---|
| `shape` | `enum: `line` / `rect` / `circle`` | `line` |
| `baseColor` | `color` | — |
| `highlightColor` | `color` | — |

## Methods

| Method | Description |
|---|---|
| `baseColor(*args, **kwargs)` | Base color. |
| `highlightColor(*args, **kwargs)` | Highlight color. |
| `shape(*args, **kwargs)` | Shape. |
| `start()` | Start. |
| `stop()` | Stop. |

## Theming

Colours come from the design tokens, so they follow the active theme. Roles used: `surface-muted`, `surface`.

See [Design tokens](../02-Theming/DesignTokens.md).

## Related

[AnalogGaugeWidget](AnalogGaugeWidget.md) · [QCustomAgendaList](QCustomAgendaList.mdx) · [QCustomAlert](QCustomAlert.md) · [QCustomAvatar](QCustomAvatar.mdx) · [QCustomAvatarGroup](QCustomAvatarGroup.mdx) · [QCustomBadge](QCustomBadge.md) · [QCustomCard](QCustomCard.md) · [QCustomChip](QCustomChip.mdx)
