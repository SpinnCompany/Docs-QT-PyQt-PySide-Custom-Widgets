---
mdx:
  format: md
---

<!-- generated:widget-reference -->
# QCustomGradientText

![QCustomGradientText screenshot](/img/showcase/gradienttext.png)

`QCustomGradientText` — text filled with a gradient.

The headline treatment every landing page wants and QSS cannot do: Qt
stylesheets have no text-fill gradient, so the only route is painting the
text with a gradient pen.

Optionally animated, sliding the gradient along the text. The animation is
off by default - a permanently shimmering headline is a distraction, and it
costs a repaint per frame.

Reuses the multi-stop CSV convention from QCustomGradientPicker, so a
gradient authored there drops straight in.

---

## Import

```python
from Custom_Widgets.QCustomGradientText import QCustomGradientText
```

Also available from the **Qt Designer** palette — every property below is settable in Designer and saved into the `.ui` file.

## Constructor

```python
QCustomGradientText(parent=None, text='', stops=None)
```

## Properties

| Property | Type | Default |
|---|---|---|
| `text` | `string` | — |
| `stopsCsv` | `string` | `0:#2563eb,1:#a855f7` |
| `angle` | `int` | `0` |
| `animated` | `bool` | `False` |
| `animationSpeed` | `int` | `40` |
| `fontScale` | `float` | `1.6` |
| `bold` | `bool` | `True` |
| `alignment` | `enum: `left` / `center` / `right`` | `center` |
| `wordWrap` | `bool` | `False` |

## Methods

| Method | Description |
|---|---|
| `alignment(*args, **kwargs)` |  |
| `angle(*args, **kwargs)` |  |
| `animated(*args, **kwargs)` |  |
| `animationSpeed(*args, **kwargs)` |  |
| `bold(*args, **kwargs)` |  |
| `clicked(...)` |  |
| `fontScale(*args, **kwargs)` |  |
| `gradientFor(rect)` | The gradient across a rect, including the animation offset. |
| `isAnimating()` |  |
| `setStops(stops)` | Replace the gradient stops. Fewer than two is rejected. |
| `start()` |  |
| `stop()` |  |
| `stops()` |  |
| `stopsCsv(*args, **kwargs)` |  |
| `text(*args, **kwargs)` |  |
| `wordWrap(*args, **kwargs)` |  |

## Signals

| Signal | Description |
|---|---|
| `clicked()` |  |

## Theming

Colours come from the design tokens, so they follow the active theme. Roles used: `accent`.

See [Design tokens](../02-Theming/DesignTokens.md).

### Dark theme

![QCustomGradientText dark](/img/showcase/gradienttext-dark.png)

## Example

A runnable demo ships with the library:

```bash
python examples/PySide6/QCustomGradientText/main.py
```

---

<!-- Generated by tools/gen_widget_docs.py from the widget's __catalog__, metaObject and module docstring. Edit the widget, then regenerate — hand edits here are overwritten. Remove the marker at the top of this file to take it over by hand. -->
