---
mdx:
  format: md
---

<!-- generated:widget-reference -->
# QCustomTypingIndicator

![QCustomTypingIndicator screenshot](/img/showcase/typingindicator.png)

`QCustomTypingIndicator` — the animated "… is typing" dots.

Three dots that bounce/fade in sequence, painted with QPainter (no assets),
optionally inside a chat-bubble-style pill. Call start()/stop() (or set the
`running` property) to animate; colour and dot size are qproperties.
Designer-droppable so it drops straight into a thread.

---

## Import

```python
from Custom_Widgets.QCustomTypingIndicator import QCustomTypingIndicator
```

Also available from the **Qt Designer** palette — every property below is settable in Designer and saved into the `.ui` file.

## Constructor

```python
QCustomTypingIndicator(parent=None)
```

## Properties

| Property | Type | Default |
|---|---|---|
| `running` | `bool` | `True` |
| `dotColor` | `color` | `#8a93a6` |
| `dotSize` | `int` | `7` |
| `bubble` | `bool` | `True` |
| `bubbleColor` | `color` | `#eef1f5` |

## Methods

| Method | Description |
|---|---|
| `bubble(*args, **kwargs)` |  |
| `bubbleColor(*args, **kwargs)` |  |
| `dotColor(*args, **kwargs)` |  |
| `dotSize(*args, **kwargs)` |  |
| `running(*args, **kwargs)` |  |
| `start()` |  |
| `stop()` |  |

## Theming

Colours come from the design tokens, so they follow the active theme. Roles used: `accent`.

See [Design tokens](../02-Theming/DesignTokens.md).

### Dark theme

![QCustomTypingIndicator dark](/img/showcase/typingindicator-dark.png)

---

<!-- Generated by tools/gen_widget_docs.py from the widget's __catalog__, metaObject and module docstring. Edit the widget, then regenerate — hand edits here are overwritten. Remove the marker at the top of this file to take it over by hand. -->
