---
mdx:
  format: md
---

<!-- generated:widget-reference -->
# QCustomColorPicker

![QCustomColorPicker screenshot](/img/showcase/colorpicker.png)

`QCustomColorPicker` — a colour selector.

A colour swatch + hex field. Clicking the swatch opens a popup of preset
swatches with a "Custom..." button (QColorDialog). Emits colorChanged.

---

## Import

```python
from Custom_Widgets.QCustomColorPicker import QCustomColorPicker
```

Also available from the **Qt Designer** palette — every property below is settable in Designer and saved into the `.ui` file.

## Constructor

```python
QCustomColorPicker(parent=None, color='#3b82f6')
```

## Methods

| Method | Description |
|---|---|
| `color()` |  |
| `colorChanged(...)` |  |
| `colorName()` |  |
| `setColor(color)` |  |

## Signals

| Signal | Description |
|---|---|
| `colorChanged(QColor)` |  |

## Theming

Colours come from the design tokens, so they follow the active theme. Roles used: `surface`, `on-surface`, `surface-muted`, `outline`, `accent`.

See [Design tokens](../02-Theming/DesignTokens.md).

### Dark theme

![QCustomColorPicker dark](/img/showcase/colorpicker-dark.png)

---

<!-- Generated by tools/gen_widget_docs.py from the widget's __catalog__, metaObject and module docstring. Edit the widget, then regenerate — hand edits here are overwritten. Remove the marker at the top of this file to take it over by hand. -->
