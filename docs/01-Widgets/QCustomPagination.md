---
mdx:
  format: md
---

<!-- generated:widget-reference -->
# QCustomPagination

![QCustomPagination screenshot](/img/showcase/pagination.png)

`QCustomPagination` — page navigation with prev/next and ellipsis.

[<] [1] ... [4] [5] [6] ... [20] [>]  - first/last always shown, a window
around the current page, ellipsis for gaps. Tokenized. Emits pageChanged.

---

## Import

```python
from Custom_Widgets.QCustomPagination import QCustomPagination
```

Also available from the **Qt Designer** palette — every property below is settable in Designer and saved into the `.ui` file.

## Constructor

```python
QCustomPagination(parent=None, pageCount=1)
```

## Methods

| Method | Description |
|---|---|
| `currentPage()` |  |
| `pageChanged(...)` |  |
| `pageCount()` |  |
| `setCurrentPage(page)` |  |
| `setPageCount(count)` |  |

## Signals

| Signal | Description |
|---|---|
| `pageChanged(int)` |  |

## Theming

Colours come from the design tokens, so they follow the active theme. Roles used: `surface`, `on-surface`, `surface-muted`, `outline`, `accent`, `on-primary`.

See [Design tokens](../02-Theming/DesignTokens.md).

### Dark theme

![QCustomPagination dark](/img/showcase/pagination-dark.png)

---

<!-- Generated by tools/gen_widget_docs.py from the widget's __catalog__, metaObject and module docstring. Edit the widget, then regenerate — hand edits here are overwritten. Remove the marker at the top of this file to take it over by hand. -->
